export const isValidBday = (bday) => {
    // date is a string on client side
    // convert to date 
    // check to make sure date cant be too far back or more than current year
    // if year is more than current year, error
    // if year is too far back, error
    // if month is over 12 error
    // if day is over 31 error - depending on the month
}